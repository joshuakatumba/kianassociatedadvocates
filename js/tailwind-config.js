tailwind = {
    config: {
        theme: {
            extend: {
                colors: {
                    brand: {
                        bg: '#f3f1eb', // Light beige background
                        navy: '#1d2736', // Deep navy blue
                        gold: '#b2955f', // Gold accent
                        gray: '#6b7280', // Text gray
                        light: '#ffffff',
                        dark: '#151c27' // Slightly darker navy for sections
                    }
                },
                fontFamily: {
                    sans: ['Inter', 'sans-serif'],
                    serif: ['Playfair Display', 'serif'],
                }
            }
        }
    }
}
